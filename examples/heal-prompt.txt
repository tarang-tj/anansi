The 'hours' field(s) stopped extracting and now return empty. 'hours' was populated in 100% of records across 6 prior runs, with values like "Mon-Fri 9am-4pm"; "Mon-Fri 9am-4pm, Wed 9am-7pm"; "Mon-Fri 10am-2pm". The page still loads normally (HTTP 200, 30201 bytes of HTML). These fields still extract correctly: address, eligibility, languages, name, phone, services. The information is still on the page but has moved. Re-locate the 'hours' field(s) and restore extraction to the original output shape.
